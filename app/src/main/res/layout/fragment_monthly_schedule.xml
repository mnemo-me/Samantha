<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.monthly_schedule.MonthlyScheduleFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/monthly_schedule_month"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/default_padding"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="@dimen/default_padding"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:text="March"
            tools:text="March"/>


        <include
            android:id="@+id/month_schedule_header"
            layout="@layout/monthly_schedule_header"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/default_padding"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/monthly_schedule_month"/>

        <GridView
            android:id="@+id/month_schedule_schedule"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/month_schedule_header"
            android:layout_marginStart="@dimen/default_padding"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="@dimen/default_padding"
            android:verticalSpacing="@dimen/quarter_padding"
            android:horizontalSpacing="@dimen/quarter_padding"
            android:numColumns="7"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
